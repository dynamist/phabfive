[tools]
# Local Phorge in the shared k3d cluster, keep the same pins as the other apps in it
k3d = "5.9.0"
# Keep kubectl in lockstep with the k3s image in k8s/cluster/k3d.yaml (max ±1 minor).
# That file is a shared contract -- bump it in dynatron and noodle at the same time.
kubectl = "1.37.0"
kubeconform = "0.8.0"
